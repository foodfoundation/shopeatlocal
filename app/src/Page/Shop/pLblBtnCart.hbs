{{!-- pLblBtnCart.hbs
//    ---------------
//    Cart button label content --}}

{{#if FlagShop}}

    {{#if (hCkAnd SummCartImperUser SummCartImperUser.QtyProm)}}
        {{hTextCurrOrOrig SummCartImperUser.Ttl}} {{t "common:cart.in"}}<br>
        {{hDeterminerUser CredImperUser false}} {{t "common:cart.cartLower"}}
        {{#if ShowFirstItemInCartNotice}}
            <script>
                $(document).ready(function() {
                    $('#DlgNotice').modal('show');
                });
            </script>
        {{/if}}
    {{else}}
        {{hDeterminerUser CredSelImperUser true}} {{t "common:cart.cartIsEmpty"}}
    {{/if}}

{{else}}
    {{t "common:cart.theCartIsClosed"}}
{{/if}}
